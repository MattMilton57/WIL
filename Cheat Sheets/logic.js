// If/Else logic
handleType = (e) => {
    if ((e.key) == "a") {resize('+')}
  else 
    if ((e.key) == "s") {resize('-')} 
  }

  //if logic
  functionName = () => {
    if (conditional)
        {console.log("do this thing")}
    }



  